# 美食旅游综合平台实验报告

---

## 三、实验步骤

### 3.1 软件功能介绍

本作品是一款基于 uni-app 框架开发的**美食旅游综合平台**，集美食推荐、旅游景点、酒店住宿、休闲娱乐、特产商城、旅游路线六大模块于一体，为用户提供一站式的"吃喝玩乐"服务体验。

**核心功能模块：**

| 模块 | 功能描述 |
|------|---------|
| **引导页** | 首次启动展示4屏引导，介绍平台核心功能 |
| **首页** | 轮播图、8个分类导航、城市筛选、猜你喜欢、热门景点推荐 |
| **分类浏览** | 按美食/景点/酒店/休闲/特产/路线/火锅/小吃分类浏览 |
| **商品详情** | 商品信息展示、用户评价、收藏、加入购物车、立即购买 |
| **购物车** | 商品数量管理、批量结算、生成订单 |
| **订单系统** | 订单状态流转（待付款→待使用→已完成）、取消/支付/使用/再来一单 |
| **评价系统** | 星级评分、文字评价、图片上传、快捷标签、匿名发布、编辑/删除 |
| **用户登录** | 手机号+验证码模拟登录 |
| **个人中心** | 个人资料编辑、头像更换 |
| **收藏功能** | 收藏/取消收藏，收藏列表查看 |
| **浏览足迹** | 自动记录浏览历史，支持清空 |
| **优惠券** | 8种优惠券领取 |
| **收货地址** | 地址的增删改查、默认地址设置 |
| **客服中心** | FAQ折叠展开、一键拨号、微信复制 |
| **设置** | 缓存清除、通知开关、版本信息 |

### 3.2 设计思路

**（1）技术选型**

采用 **uni-app + Vue 2 Options API** 框架，一套代码可同时发布到 H5、微信小程序和 App 端。数据存储使用 `uni.setStorageSync` / `uni.getStorageSync` 本地缓存方案，无需后端服务器即可实现完整的业务逻辑。

**（2）架构设计**

```
App.vue (应用入口，引导页判断)
  ├── pages/index/      首页（Tab）
  ├── pages/orders/     订单（Tab）
  ├── pages/mine/       我的（Tab）
  ├── pages/detail/     详情页
  ├── pages/category/   分类列表
  ├── pages/cart/       购物车
  ├── pages/comment/    写评价
  ├── pages/login/      登录
  ├── pages/favorites/  收藏
  ├── pages/history/    足迹
  ├── pages/coupons/    优惠券
  ├── pages/address/    地址管理
  ├── pages/profile/    个人资料
  ├── pages/settings/   设置
  ├── pages/service/    客服
  ├── pages/about/      关于
  └── common/mock.js    模拟数据（207条商品/景点等）
```

**（3）数据模型设计**

mock.js 中定义 6 大数据集，共计 207 条数据：
- **foodList**（美食，88条）：按城市分类，涵盖北京烤鸭、兰州拉面、重庆火锅等各地特色美食
- **travelList**（景点，95条）：涵盖故宫、九寨沟、黄山等全国知名景点
- **hotelList**（酒店，6条）：精品酒店示例
- **leisureList**（休闲娱乐，6条）：KTV、密室逃脱等
- **specialtyList**（特产，6条）：各地特产商品
- **routeList**（旅游路线，6条）：多日游线路

每条数据包含 id、name、image、rating、price、location、description、tags 等字段。

**（4）数据流设计**

所有用户数据（购物车、订单、收藏、评价、足迹、地址、优惠券等）均通过 `uni.setStorageSync` 持久化到本地存储。各页面通过读写相同的 storage key 实现数据共享，形成一个闭环的用户数据流。

### 3.3 流程图

```
用户启动App
    │
    ▼
┌──────────┐  首次  ┌──────────┐
│  引导页   │◄──────│ App.vue  │
└────┬─────┘       └────┬─────┘
     │ 开始体验          │ 非首次
     ▼                  ▼
┌──────────────────────────────────────┐
│              首页 (Tab)               │
│  ┌─────────┐  ┌─────────┐  ┌──────┐ │
│  │ 城市筛选  │  │ 轮播图   │  │ 分类  │ │
│  │ 猜你喜欢  │  │ 热门景点  │  │ 搜索  │ │
│  └─────────┘  └─────────┘  └──────┘ │
└────────┬─────────────────────────────┘
         │ 点击
         ▼
┌──────────────┐     ┌──────────────┐
│   详情页       │────►│   登录页      │（未登录拦截）
│ 商品/景点信息  │     └──────────────┘
│ 收藏/加购/购买 │
└──┬────┬───┬──┘
   │    │   │
   ▼    ▼   ▼
┌────┐┌────┐┌────────┐
│收藏││购物车││下单面板 │
└────┘└──┬─┘└───┬────┘
         │      │
         ▼      ▼
     ┌──────────────┐
     │   订单页 (Tab) │
     │ 待付款→待使用  │
     │ →已完成→评价  │
     └──────────────┘
           │
           ▼
     ┌──────────────┐
     │   评价页       │
     │ 评分/文字/图片 │
     └──────────────┘

┌──────────────────────────────┐
│         我的 (Tab)            │
│ 个人资料│收藏│足迹│优惠券     │
│ 地址管理│客服│设置│关于       │
└──────────────────────────────┘
```

---

## 四、实验内容

### 4.1 项目结构

```
美食旅游uniapp/
├── App.vue                    # 应用入口，全局样式
├── main.js                    # Vue实例挂载
├── pages.json                 # 页面路由与TabBar配置
├── manifest.json              # 应用配置（appid/版本/权限）
├── index.html                 # H5入口HTML
├── .gitignore                 # Git忽略规则
├── common/
│   └── mock.js                # 全部模拟数据（207条+订单+优惠券）
├── pages/
│   ├── guide/guide.vue        # 引导页
│   ├── index/index.vue        # 首页（Tab）+ 城市筛选
│   ├── detail/detail.vue      # 商品/景点详情 + 下单
│   ├── category/category.vue  # 分类列表
│   ├── cart/cart.vue          # 购物车
│   ├── orders/orders.vue      # 订单列表（Tab）
│   ├── mine/mine.vue          # 个人中心（Tab）
│   ├── login/login.vue        # 手机号登录
│   ├── comment/comment.vue    # 写/编辑评价
│   ├── my-comments/my-comments.vue  # 我的评价
│   ├── favorites/favorites.vue      # 我的收藏
│   ├── history/history.vue          # 浏览足迹
│   ├── coupons/coupons.vue          # 优惠券
│   ├── address/address.vue          # 收货地址
│   ├── profile/profile.vue          # 个人资料
│   ├── settings/settings.vue        # 设置
│   ├── service/service.vue          # 客服中心
│   └── about/about.vue              # 关于我们
└── static/
    ├── tabbar/                # 底部导航图标
    └── images/
        ├── food/              # 美食相关图片
        └── travel/            # 景点本地照片（6张）
```

### 4.2 核心源代码（含注释）

#### 4.2.1 应用入口 App.vue

```javascript
// App.vue - 应用入口，判断首次启动
export default {
    onLaunch() {
        // 读取引导页标记，判断是否首次启动
        const hasGuided = uni.getStorageSync('hasGuided');
        if (!hasGuided) {
            // 首次启动 -> 跳转引导页
            uni.reLaunch({ url: '/pages/guide/guide' });
        }
    }
};
```

#### 4.2.2 首页核心逻辑（城市筛选 + 数据加载）

```javascript
// pages/index/index.vue
data() {
    return {
        currentCity: '全部',           // 当前选中城市，默认显示全部
        banners: banners,              // 轮播图数据
        categories: homeCategories,    // 8个分类
        foodList: [],                  // 过滤后的美食列表
        travelList: [],                // 过滤后的景点列表
        cartCount: 0,                  // 购物车角标数量
        allFoods: foodList,            // 全部美食（用于过滤）
        allTravels: travelList         // 全部景点（用于过滤）
    };
},
methods: {
    // 城市筛选：根据location字段前缀匹配
    filterByCity() {
        let foods = this.allFoods;
        let travels = this.allTravels;
        if (this.currentCity && this.currentCity !== '全部') {
            const f = foods.filter(item =>
                item.location.startsWith(this.currentCity)
            );
            if (f.length > 0) foods = f;
            const t = travels.filter(item =>
                item.location.startsWith(this.currentCity)
            );
            if (t.length > 0) travels = t;
        }
        this.foodList = foods.slice(0, 8);    // 最多显示8条
        this.travelList = travels.slice(0, 8);
    }
}
```

#### 4.2.3 详情页下单逻辑

```javascript
// pages/detail/detail.vue
methods: {
    // 提交订单：生成订单数据并持久化
    submitOrder() {
        const order = {
            id: Date.now(),                    // 使用时间戳作为唯一订单号
            itemId: this.item.id,
            type: this.itemType,
            itemName: this.item.name,
            itemImage: this.item.image,
            price: this.item.price,
            count: this.checkoutQty,           // 购买数量
            total: this.totalPrice,            // 合计金额
            status: 'pending',                 // 初始状态：待付款
            statusText: '待付款',
            date: this.formatDate(),
            code: Math.random().toString(36)
                .substring(2, 10).toUpperCase() // 随机订单号
        };
        const orders = uni.getStorageSync('orders') || [];
        orders.unshift(order);                 // 新订单放在最前面
        uni.setStorageSync('orders', orders);
    }
}
```

#### 4.2.4 登录页验证码倒计时

```javascript
// pages/login/login.vue
methods: {
    sendCode() {
        if (this.countdown > 0) return;
        // 手机号格式验证
        if (!/^1[3-9]\d{9}$/.test(this.phone)) {
            this.phoneError = '手机号格式不正确';
            return;
        }
        // 模拟发送验证码，启动60秒倒计时
        this.countdown = 60;
        this.timer = setInterval(() => {
            this.countdown--;
            if (this.countdown <= 0) {
                clearInterval(this.timer);
                this.timer = null;
            }
        }, 1000);
    }
}
```

#### 4.2.5 评价发布逻辑

```javascript
// pages/comment/comment.vue
methods: {
    submitComment() {
        // 校验：必须评分且内容至少5字
        if (!this.canSubmit) return;
        const allComments = uni.getStorageSync('allComments') || [];
        const comment = {
            id: Date.now(),
            itemId: this.item.id,
            itemType: this.itemType,
            user: this.isAnonymous ? '匿名用户' : this.getUserName(),
            content: this.content.trim(),
            rating: this.rating,
            tags: [...this.selectedTags],
            images: [...this.images],
            time: this.formatDate(),
            likes: 0
        };
        allComments.unshift(comment);
        uni.setStorageSync('allComments', allComments);
    }
}
```

#### 4.2.6 订单状态流转

```javascript
// pages/orders/orders.vue
methods: {
    // 模拟支付：待付款 -> 待使用
    payOrder(order) {
        order.status = 'used';
        order.statusText = '待使用';
        this.updateOrders();
    },
    // 确认使用：待使用 -> 已完成
    useOrder(order) {
        order.status = 'completed';
        order.statusText = '已完成';
        this.updateOrders();
    },
    // 取消订单
    cancelOrder(order) {
        uni.showModal({
            title: '取消订单',
            content: '确定取消该订单吗？',
            success: (res) => {
                if (res.confirm) {
                    const orders = uni.getStorageSync('orders') || [];
                    const newOrders = orders.filter(o => o.id !== order.id);
                    uni.setStorageSync('orders', newOrders);
                    this.orderList = this.filterOrders(newOrders);
                }
            }
        });
    }
}
```

#### 4.2.7 数据模型示例（mock.js）

```javascript
// common/mock.js - 美食数据示例
export const foodList = [
    {
        id: 1,
        name: '北京烤鸭',
        image: 'https://picsum.photos/seed/food1/400/300',
        rating: 4.8,
        price: 188,
        originalPrice: 268,
        sales: 12580,
        category: '特色菜',
        location: '北京市',
        description: '北京烤鸭是北京名食，以色泽红艳、肉质细嫩...',
        tags: ['必吃榜', '老字号', '非遗美食'],
        comments: [
            {
                user: '美食达人小王',
                avatar: '',
                content: '皮脆肉嫩，味道正宗！',
                rating: 5,
                time: '2025-12-20',
                images: []
            }
        ]
    },
    // ... 共88条美食数据
];
```

---

## 五、实验结果

### 5.1 运行结果

在 HBuilder 3.99 开发环境中，项目成功编译运行于 H5 端。以下为各核心功能的运行效果：

**(请在此处插入各页面的运行截图)**

| 页面 | 功能验证结果 | 截图位置 |
|------|-------------|---------|
| 引导页 | 首次启动展示4屏引导，跳过/下一步/开始体验按钮正常 | [插入截图] |
| 首页 | 城市筛选、轮播图、分类导航、猜你喜欢、热门景点均正常显示 | [插入截图] |
| 分类页 | 8种分类全部可正常浏览，数据正确加载 | [插入截图] |
| 详情页 | 商品信息、评价展示、收藏、加购、下单功能正常 | [插入截图] |
| 登录页 | 手机号验证、验证码倒计时、登录/登出正常 | [插入截图] |
| 购物车 | 数量调整、删除、批量结算正常 | [插入截图] |
| 订单页 | 订单状态流转（待付款→待使用→已完成）正常 | [插入截图] |
| 评价系统 | 评分、文字输入、图片上传、标签选择、匿名发布正常 | [插入截图] |
| 个人中心 | 信息展示、各功能入口跳转正常 | [插入截图] |
| 收藏/足迹 | 收藏切换、浏览记录自动保存、清空功能正常 | [插入截图] |
| 优惠券 | 8张优惠券展示、领取功能正常 | [插入截图] |
| 地址管理 | 新增/编辑/删除/设为默认功能正常 | [插入截图] |
| 客服中心 | FAQ折叠展开、拨号、复制功能正常 | [插入截图] |
| 设置 | 缓存清除、通知开关持久化正常 | [插入截图] |

### 5.2 结果分析

**（1）城市筛选功能验证**

选择"北京"后，首页仅显示 location 以"北京市"开头的美食和景点；选择"全部"则显示所有城市数据。该功能通过 `location.startsWith(city)` 方法实现，逻辑正确但只能按前缀匹配，若某城市有市辖区数据（如"海淀区"）也需 location 以"北京市"开头。目前 207 条数据中 location 均以城市名称开头，符合设计。

**（2）订单生命周期验证**

订单状态从"待付款"→"待使用"→"已完成"完整流转，取消订单可正确从列表中移除。"再来一单"功能可复制原订单并以新状态创建。"去评价"可正确跳转到评价页并关联对应商品。

**（3）数据持久化验证**

刷新浏览器后，购物车、订单、收藏、评价、足迹、地址等所有用户数据均保持不丢失，验证了 `uni.setStorageSync` 的持久化可靠性。

**（4）登录拦截验证**

未登录状态下，点击"加入购物车"和"立即购买"均弹出登录确认框，引导至登录页。登录后自动返回来源页，登录状态正确维护。

**（5）已知限制**

- 图片主要使用 picsum.photos 外部 CDN 随机图片，仅 6 个旅游景点有本地真实照片，其他图片与实际菜品/景点不完全对应
- 搜索功能仅展示占位提示，未实现关键词搜索
- 微信和支付宝登录显示"功能开发中"，未实际接入
- 无后端 API，所有为前端模拟数据
- 验证码为模拟发送，未对接真实短信服务

---

## 六、总结

### 6.1 实验收获

**（1）掌握了 uni-app 跨平台开发**

通过本次实验，深入学习了 uni-app 框架的使用，包括页面路由配置（pages.json）、组件化开发（.vue 单文件组件）、条件编译、本地存储 API 等核心技术。一套代码即可运行在 H5、小程序和 App 端的跨平台特性极大提高了开发效率。

**（2）深入理解了 Vue 2 组件化思想**

项目采用 Options API 组织代码，通过 data、computed、methods、生命周期钩子等选项管理组件状态和逻辑。在开发过程中修复了多个 Vue 2 模板语法问题（模板中不允许使用 `++`、`--`、`=` 等语句），加深了对 Vue 编译机制的理解。

**（3）实践了前端数据管理与状态持久化**

在没有后端的情况下，通过 `uni.setStorageSync` 实现了购物车、订单、收藏、评价、足迹等完整的数据闭环。理解了如何在多个页面间通过共享 storage key 实现数据流转，体验了"前端全栈"的开发模式。

**（4）学习了移动端 UI 设计规范**

设计了统一的橙色 (`#FF6600`) 主色调，采用卡片式布局、渐变按钮、底部弹出面板等移动端常见交互模式，使应用具有较好的视觉一致性和用户体验。

### 6.2 体会与反思

本次实验从零搭建了一个功能完善的综合平台，涵盖 18 个页面、207 条模拟数据、10 余个功能模块。最大的挑战在于**数据一致性维护**——当多个页面同时读写同一份 storage 数据时，需要确保每个页面在 `onShow` 生命周期中重新读取最新数据，避免展示过期状态。

此外，GitHub 推送过程中遇到的网络问题也让我意识到，在实际项目中网络环境和工具链的稳定性同样重要。最终通过 GitHub API 完成了代码托管。

通过此项目，不仅巩固了 uni-app 和 Vue 技术栈的实践能力，也积累了完整移动端应用的架构设计经验，为今后开发真实商业项目打下基础。
